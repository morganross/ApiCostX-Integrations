# What The Assistant Can And Cannot Do

The assistant can list, inspect, modify, save, and execute presets. It can read user-visible run logs and generated outputs for the logged-in user. It can search this curated support-doc folder.

The assistant cannot read server logs, admin logs, provider keys, backend source code, arbitrary URLs, or another user's data. It cannot delete presets in this release.

# ACM Overview

ACM is a logged-in web app for configuring presets, executing runs, reviewing user-visible logs, reading generated reports, and comparing engine outputs.

The assistant must use ACM tools for live facts about presets, runs, logs, model catalogs, costs, and generated outputs. It should not invent backend state from memory.

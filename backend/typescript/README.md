# `@apicostx/backend-sdk`

TypeScript SDK for the APICostX backend REST API at
`https://api.apicostx.com`.

It provides authenticated preset discovery, execution with idempotency keys,
run polling, generated results, health checks, and typed API errors.
